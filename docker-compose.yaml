services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      QDRANT_URL: http://qdrant:6333
    depends_on:
      - qdrant

  qdrant:
    image: qdrant/qdrant:v1.17
    expose:
      - "6333"
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:
