services:
  api:
    build: .
    ports:
      - "8000:8000"

  qdrant:
    image: qdrant/qdrant:v1.17
    expose:
      - "6333"
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:
